solutions = [
  { "name"        : "mid-b",
    "url"         : "git@github.com:lbrownell-gpsw/mid-b-ljb.git",
    "deps_file"   : "DEPS",
    "managed"     : False,
    # custom_deps: (optional) -- override the dependencies specified in the deps and deps_os variables in child DEPS files. 
    "custom_deps" : {
    },
    # custom_vars (optional) -- override the variables defined in vars in child DEPS files.
    "custom_vars" : "",
    "safesync_url": "",
  },
]
cache_dir = None
